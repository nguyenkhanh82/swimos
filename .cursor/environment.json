{
  "name": "SwimOS",
  "install": "bash .cursor/install.sh",
  "start": "true",
  "chromeExecutablePath": "/usr/bin/google-chrome-stable",
  "terminals": [
    {
      "name": "SwimScraper API",
      "command": "cd SwimScraper && source venv/bin/activate && cd api && uvicorn main:app --host 0.0.0.0 --port 8000",
      "description": "FastAPI service (SwimScraper) on http://localhost:8000 — see /health and /docs."
    },
    {
      "name": "swim-tracker web",
      "command": "cd swim-tracker && flutter run -d web-server --web-port=8080 --web-hostname=0.0.0.0",
      "description": "Flutter web dev server for swim-tracker on http://localhost:8080."
    }
  ],
  "ports": [
    { "name": "SwimScraper API", "port": 8000 },
    { "name": "swim-tracker web", "port": 8080 }
  ]
}
